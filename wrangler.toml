# Cloudflare Pages config for Wrangler.
# In the Cloudflare dashboard, keep these settings:
# Build command: npm run build
# Build output directory: dist
# Root directory: /
pages_build_output_dir = "dist"
compatibility_date = "2026-05-02"
